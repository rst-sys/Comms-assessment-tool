{
  "name": "accountable-communications-review",
  "version": "0.1.0",
  "private": true,
  "description": "Accountable Communications Review — does this message give an account?",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "fixtures": "tsx scripts/run-fixtures.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
